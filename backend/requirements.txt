# PhishGuard Lambda runtime dependencies
#
# Optimized for serverless deployment size:
# - native XGBoost JSON model loading
# - no Flask / scikit-learn / joblib runtime dependency chain
numpy>=1.24.0
xgboost>=2.0.0
python-whois>=0.9.0
dnspython>=2.4.0
